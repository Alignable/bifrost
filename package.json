{
  "description": "Bridge from Turbolinks to Vite",
  "private": true,
  "workspaces": [
    "bifrost",
    "bifrost-fastify",
    "tests/*"
  ],
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "scripts": {
    "test": "yarn workspace tests-playwright test",
    "build": "yarn workspace bifrost build && yarn workspace bifrost-fastify build"

  }
}
