{
  "description": "Bridge from Turbolinks to Vite",
  "workspaces": [
    "bifrost",
    "bifrost-fastify",
    "tests/*"
  ],
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "scripts": {
    "test": "npm tests-playwright --workspace test",
    "build": "npm run build --workspace=bifrost --workspace bifrost-fastify"
  },
  "publishConfig": {
    "@alignable:registry": "https://npm.pkg.github.com"
  }
}
