{
  "description": "Bridge from Turbolinks to Vite",
  "workspaces": [
    "bifrost",
    "bifrost-fastify",
    "tests/*"
  ],
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "scripts": {
    "test": "npm run test --workspace tests-playwright",
    "build": "npm run build --workspace=bifrost --workspace bifrost-fastify"
  }
}
