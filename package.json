{
  "name": "bifrost",
  "description": "Bridge from Turbolinks to Vite",
  "workspaces": [
    "bifrost",
    "bifrost-fastify",
    "tests/*"
  ],
  "scripts": {
    "test": "npm run -w=tests-playwright test",
    "build": "npm run -w=bifrost -w=bifrost-fastify build",
    "publish": "npm -w=bifrost -w=bifrost-fastify publish --access public"
  }
}
