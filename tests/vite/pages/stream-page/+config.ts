import { MainNavLayout } from "../../layouts/MainNavLayout";
import { Config } from "vike/types";

export default {
  Layout: MainNavLayout,
  title: "stream page",
  stream: true,
} satisfies Config;
